accounts:
  root:
    first-name: Lvoxx
    last-name: Aegg Srtist
    # email: <change-me>    <- ! CAUTION: Only using this if not passsing env from docker compose
    # password: <change-me> <- ! CAUTION: Only using this if not passsing env from docker compose
    locked: false
    enabled: true
    points: 999999999
    roles: ["ADMIN"]
